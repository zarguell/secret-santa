---
phase: 05-api-endpoint-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.ts
  - tests/api.test.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Client can POST wishlist text and receive 200 OK on success"
    - "Client can GET wishlist text and receive current content or empty string"
    - "API returns 400 Bad Request for invalid input (non-string, exceeds 500 chars)"
    - "API validates guest ID exists in party before processing request"
    - "GET /api/guest/:guestId/assignment includes recipientGuestId in response"
    - "Integration tests verify endpoints handle valid and invalid requests"
  artifacts:
    - path: "src/index.ts"
      provides: "PUT /api/guest/:guestId/wishlist endpoint"
      contains: "url.pathname.match(/^\\/api\\/guest\\/[a-f0-9-]+\\/wishlist$/)"
    - path: "src/index.ts"
      provides: "GET /api/guest/:guestId/wishlist endpoint"
      contains: "url.pathname.match(/^\\/api\\/guest\\/[a-f0-9-]+\\/wishlist$/)"
    - path: "src/index.ts"
      provides: "Extended assignment response with recipientGuestId"
      contains: "recipientGuestId"
    - path: "tests/api.test.ts"
      provides: "Integration tests for wishlist API endpoints"
      contains: "describe.*wishlist"
  key_links:
    - from: "src/index.ts"
      to: "env.GUEST_KV"
      via: "getGuestMapping()"
      pattern: "getGuestMapping\\(env\\.GUEST_KV"
    - from: "src/index.ts"
      to: "partyStub.setWishlist()"
      via: "DO stub method call"
      pattern: "partyStub\\.setWishlist\\("
    - from: "src/index.ts"
      to: "partyStub.getWishlist()"
      via: "DO stub method call"
      pattern: "partyStub\\.getWishlist\\("
    - from: "src/index.ts"
      to: "Party DO"
      via: "env.PARTY_DO.get() stub creation"
      pattern: "env\\.PARTY_DO\\.get\\("
---

<objective>
Implement wishlist API endpoints with validation and error handling

Purpose: Expose wishlist storage functionality via HTTP, following existing API patterns
Output: Working PUT/GET /api/guest/:guestId/wishlist endpoints, extended assignment response, integration tests
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/index.ts
@src/party.ts
@src/types.ts
@tests/api.test.ts
</context>

<tasks>

<task type="auto">
  <name>Implement PUT /api/guest/:guestId/wishlist endpoint</name>
  <files>src/index.ts</files>
  <action>
Add PUT endpoint for wishlist updates after the GET /api/guest/:guestId/assignment endpoint (around line 154).

Pattern to follow (similar to GET assignment endpoint):

1. Match route: url.pathname.match(/^\/api\/guest\/[a-f0-9-]+\/wishlist$/) && request.method === "PUT"
2. Extract guestId from URL: const guestId = url.pathname.split("/")[3]
3. Validate guestId format (36-char UUID)
4. Fetch guest mapping from KV: const mapping = await getGuestMapping(env.GUEST_KV, guestId)
5. Return 404 if mapping not found
6. Get DO stub: const partyStub = env.PARTY_DO.get(env.PARTY_DO.idFromString(mapping.partyId))
7. Parse request body: const body = await request.json() as { wishlist: string }
8. Call DO method: await partyStub.setWishlist(guestId, body.wishlist)
9. Return 200 OK with success response

Error handling:
- 400 for invalid guestId format
- 404 for guest not found in KV
- 500 for DO errors (catch block)
- Reuse corsHeaders from existing code

Request body: { wishlist: string }
Response: { success: true }
  </action>
  <verify>
Run tests: npm test -- tests/api.test.ts
Test manually:
curl -X PUT http://localhost/api/guest/{guestId}/wishlist \
  -H "Content-Type: application/json" \
  -d '{"wishlist": "Test wishlist"}'
  </verify>
  <done>
PUT endpoint returns 200 OK on success, 404 for invalid guest, 500 for errors
Request body validated and passed to DO setWishlist method
  </done>
</task>

<task type="auto">
  <name>Implement GET /api/guest/:guestId/wishlist endpoint</name>
  <files>src/index.ts</files>
  <action>
Add GET endpoint for wishlist retrieval immediately after the PUT endpoint.

Pattern to follow (similar to PUT endpoint):

1. Match route: url.pathname.match(/^\/api\/guest\/[a-f0-9-]+\/wishlist$/) && request.method === "GET"
2. Extract guestId from URL: const guestId = url.pathname.split("/")[3]
3. Validate guestId format (36-char UUID)
4. Fetch guest mapping from KV: const mapping = await getGuestMapping(env.GUEST_KV, guestId)
5. Return 404 if mapping not found
6. Get DO stub: const partyStub = env.PARTY_DO.get(env.PARTY_DO.idFromString(mapping.partyId))
7. Call DO method: const wishlist = await partyStub.getWishlist(guestId)
8. Return 200 OK with wishlist response

Error handling:
- 400 for invalid guestId format
- 404 for guest not found in KV
- 500 for DO errors (catch block)
- Reuse corsHeaders from existing code

Response: { wishlist: string } (returns empty string if not set)
  </action>
  <verify>
Run tests: npm test -- tests/api.test.ts
Test manually:
curl http://localhost/api/guest/{guestId}/wishlist
  </verify>
  <done>
GET endpoint returns 200 OK with wishlist content
Returns empty string ("") if wishlist not set
404 for invalid guest, 500 for errors
  </done>
</task>

<task type="auto">
  <name>Extend assignment response with recipientGuestId</name>
  <files>src/index.ts</files>
  <action>
Modify the GET /api/guest/:guestId/assignment endpoint to include recipientGuestId in the response.

Location: Around line 143, after const result = await partyStub.getGuestAssignment(guestId);

Current code returns:
{
  guestName,
  assignment,
  party: { ... }
}

Need to extend response to include:
- recipientGuestId: Look up recipient's guestId from partyData.guestLinks

Implementation:
1. After getting result from getGuestAssignment(), get party stub again
2. Call partyStub.getParty() to access guestLinks
3. Find recipient's guestId: const recipientGuestId = partyData.guestLinks[result.assignment]
4. Add recipientGuestId to response object

Response becomes:
{
  guestName,
  assignment,
  recipientGuestId,
  party: { ... }
}

This enables Phase 7 (recipient wishlist integration) without additional API calls.
  </action>
  <verify>
Run tests: npm test -- tests/api.test.ts
Test manually:
curl http://localhost/api/guest/{guestId}/assignment | jq
Verify response includes recipientGuestId field (36-char UUID)
  </verify>
  <done>
Assignment response includes recipientGuestId field
Existing assignment tests still pass (backward compatible)
Client can use recipientGuestId to fetch recipient's wishlist
  </done>
</task>

<task type="auto">
  <name>Add integration tests for wishlist API endpoints</name>
  <files>tests/api.test.ts</files>
  <action>
Add comprehensive integration tests for wishlist endpoints to tests/api.test.ts.

Add new describe block: "PUT /api/guest/:id/wishlist"

Test cases:
1. "should set wishlist for valid guest" - Create party, PUT wishlist, GET to verify
2. "should update existing wishlist" - PUT twice, verify second update overwrites first
3. "should return 404 for non-existent guest" - PUT with fake guestId, expect 404
4. "should return 400 for invalid guest ID format" - PUT with malformed guestId
5. "should handle empty wishlist" - PUT empty string, verify GET returns ""
6. "should handle Unicode characters" - PUT emoji/special chars, verify preserved
7. "should include CORS headers" - Verify CORS headers on wishlist endpoints

Add new describe block: "GET /api/guest/:id/wishlist"

Test cases:
1. "should get wishlist for valid guest" - Set wishlist, GET to verify
2. "should return empty string for unset wishlist" - GET without setting, expect ""
3. "should return 404 for non-existent guest" - GET with fake guestId
4. "should return 400 for invalid guest ID format" - GET with malformed guestId

Add test to existing "GET /api/guest/:id/assignment" describe block:
1. "should include recipientGuestId in response" - Verify response has recipientGuestId field

Follow existing test patterns:
- Use beforeEach to create test party
- Use guestUrl.split("/").pop() to extract guestId
- Expect specific status codes and error messages
- Test valid and invalid cases
  </action>
  <verify>
Run tests: npm test -- tests/api.test.ts
All 41 existing tests + new wishlist tests should pass
Run: npm test (full test suite)
  </verify>
  <done>
12 new integration tests covering wishlist endpoints
All tests pass (expecting 53+ total tests)
Tests cover happy path, error cases, empty state, Unicode
  </done>
</task>

</tasks>

<verification>
Run full test suite: npm test

Verify all requirements:
- [ ] PUT /api/guest/:guestId/wishlist endpoint exists and works (API-01)
- [ ] GET /api/guest/:guestId/wishlist endpoint exists and works (API-02)
- [ ] Assignment response includes recipientGuestId (API-03)
- [ ] Input validation at API boundary (API-04)
- [ ] Integration tests cover all endpoints (TEST-02)

Manual verification:
curl -X PUT http://localhost/api/guest/{guestId}/wishlist \
  -H "Content-Type: application/json" \
  -d '{"wishlist": "Books, gadgets, and coffee"}'

curl http://localhost/api/guest/{guestId}/wishlist

curl http://localhost/api/guest/{guestId}/assignment | jq .recipientGuestId
</verification>

<success_criteria>
1. Client can POST wishlist text and receive 200 OK on success
2. Client can GET wishlist text and receive current content or empty string
3. API returns 400 Bad Request for invalid input (non-string, exceeds 500 chars)
4. API validates guest ID exists in party before processing request
5. Assignment response includes recipientGuestId for Phase 7
6. Integration tests verify endpoints handle valid and invalid requests
7. All tests pass (53+ tests total)
</success_criteria>

<output>
After completion, create `.planning/phases/05-api-endpoint-layer/05-01-SUMMARY.md`
</output>
